export interface InferenceModel {
  id: string
  normal_image: string
  inferred_image: string
  inference: string // TODO: search about json not string
  created_at: string // TODO: search about json not string
}